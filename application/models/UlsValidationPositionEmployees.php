<?php

/**
 * UlsValidationPositionEmployees
 * 
 * This class has been auto-generated by the Doctrine ORM Framework
 * 
 * @package    ##PACKAGE##
 * @subpackage ##SUBPACKAGE##
 * @author     ##NAME## <##EMAIL##>
 * @version    SVN: $Id: Builder.php 6820 2009-11-30 17:27:49Z jwage $
 */
class UlsValidationPositionEmployees extends BaseUlsValidationPositionEmployees
{
	function preInsert($event){
		 $CI =& get_instance();
		$CI->load->library('session');
        $this->created_by=$CI->session->userdata('username');
        $this->created_date=date('Y-m-d');
        $this->modified_by=$CI->session->userdata('username');        
        $this->modified_date=date('Y-m-d');
        $this->last_modified_id=$CI->session->userdata('user_id');
        $this->parent_org_id=$CI->session->userdata('parent_org_id');
    }
	
    function preUpdate($event){
		$CI =& get_instance();
		$CI->load->library('session');
        $this->modified_date=date('Y-m-d');
        $this->modified_by=$CI->session->userdata('username');
        $this->last_modified_id=$CI->session->userdata('user_id');
    }
	
	static function get_validation_employees_position($emp_id,$id,$pos_id){
		$CI =& get_instance();
		$CI->load->library('session');
		$compdef=array();
		if(!empty($id) && is_numeric($id)){
			$query="select b.full_name, b.employee_number, b.org_name, b.position_name,b.location_name, a.* from uls_validation_position_employees a
			left join (select employee_id,full_name,employee_number,org_name,position_name,location_name from employee_data group by employee_id)b on b.employee_id=a.employee_id	where 1 and a.parent_org_id=".$CI->session->userdata('parent_org_id')." and a.employee_id=".$emp_id." and a.position_id=".$pos_id." and a.val_id=".$id." order by a.val_id asc";
			$compdef=UlsMenu::callpdo($query);
		}
		return $compdef;
	}
	
	static function get_employees_position($id,$pos_id,$val_pos_id){
		$CI =& get_instance();
		$CI->load->library('session');
		$compdef=array();
		if(!empty($id) && is_numeric($id)){
			$query="select b.full_name, b.employee_number, b.org_name, b.position_name,b.location_name, a.* from uls_validation_position_employees a
			left join (select employee_id,full_name,employee_number,org_name,position_name,location_name from employee_data group by employee_id)b on b.employee_id=a.employee_id	where 1 and a.parent_org_id=".$CI->session->userdata('parent_org_id')." and a.val_pos_id=".$val_pos_id." and a.position_id=".$pos_id." and a.val_id=".$id." and a.diff_pos='YES' order by a.val_id asc";
			$compdef=UlsMenu::callpdo($query);
		}
		return $compdef;
	}
	
	static function get_employees_position_view($id,$pos_id,$val_pos_id){
		$CI =& get_instance();
		$CI->load->library('session');
		$compdef=array();
		if(!empty($id) && is_numeric($id)){
			$query="select b.full_name, b.employee_number, b.org_name, b.position_name,b.location_name, a.* from uls_validation_position_employees a
			left join (select employee_id,full_name,employee_number,org_name,position_name,location_name from employee_data group by employee_id)b on b.employee_id=a.employee_id	where 1 and a.parent_org_id=".$CI->session->userdata('parent_org_id')." and a.val_pos_id=".$val_pos_id." and a.position_id=".$pos_id." and a.val_id=".$id." order by a.val_id asc";
			$compdef=UlsMenu::callpdo($query);
		}
		return $compdef;
	}
	
	static function get_employees_position_count($id){
		$CI =& get_instance();
		$CI->load->library('session');
		$compdef=array();
		if(!empty($id) && is_numeric($id)){
			$query="select count(*) as numcount from uls_validation_position_employees where 1 and parent_org_id=".$CI->session->userdata('parent_org_id')." and val_id=".$id;
			$compdef=UlsMenu::callpdorow($query);
		}
		return $compdef['numcount'];
	}
}