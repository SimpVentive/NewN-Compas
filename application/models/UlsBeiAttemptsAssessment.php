<?php

/**
 * UlsBeiAttemptsAssessment
 * 
 * This class has been auto-generated by the Doctrine ORM Framework
 * 
 * @package    ##PACKAGE##
 * @subpackage ##SUBPACKAGE##
 * @author     ##NAME## <##EMAIL##>
 * @version    SVN: $Id: Builder.php 6820 2009-11-30 17:27:49Z jwage $
 */
class UlsBeiAttemptsAssessment extends BaseUlsBeiAttemptsAssessment
{
	function preInsert($event){
		$CI =& get_instance();
		$CI->load->library('session');
        $this->created_by=$CI->session->userdata('username');
        $this->created_date=date('Y-m-d');
        $this->modified_by=$CI->session->userdata('username');        
        $this->modified_date=date('Y-m-d');
        $this->last_modified_id=$CI->session->userdata('user_id');
        $this->parent_org_id=$CI->session->userdata('parent_org_id');
    }
	
    function preUpdate($event){
		$CI =& get_instance();
		$CI->load->library('session');
        $this->modified_date=date('Y-m-d');
        $this->modified_by=$CI->session->userdata('username');
        $this->last_modified_id=$CI->session->userdata('user_id');
    }

	static function getattemptvalus_beh($ass_id,$empid,$pre,$ass_test,$int_id){
		$query=" Select a.attempt_status,a.correct_ans,a.wrong_ans,a.score,a.attempt_id,a.admin_bei_comment,a.admin_bei_result from uls_bei_attempts_assessment a
		where a.employee_id=".$empid." and a.test_type='".$pre."' and a.assessment_id=".$ass_id." and a.event_id=".$ass_test." and a.instrument_id=".$int_id;
		$details=UlsMenu::callpdorow($query);
		return $details;
	}
	
	static function get_attempt_valus_beh($ass_id,$empid,$ass_test){
		$query=" Select a.attempt_status,a.correct_ans,a.wrong_ans,a.score,a.attempt_id,a.admin_bei_comment,a.admin_bei_result,a.instrument_id from uls_bei_attempts_assessment a
		where a.employee_id=".$empid." and a.assessment_id=".$ass_id." and a.event_id=".$ass_test;
		$details=UlsMenu::callpdorow($query);
		return $details;
	}
	
	static function get_assessment_bie_employees_count($emp_id,$id,$pos_id){
		$CI =& get_instance();
		$CI->load->library('session');
		$compdef=array();
		if(!empty($id) && is_numeric($id)){
			$query="select count(b.employee_id) as count_emp from uls_assessment_employees a
			left join (SELECT `attempt_id`,`employee_id`,`assessment_id` FROM `uls_bei_attempts_assessment`)b on a.employee_id=b.employee_id and a.assessment_id=b.assessment_id	where a.parent_org_id=".$CI->session->userdata('parent_org_id')." and a.employee_id=".$emp_id." and a.position_id=".$pos_id." and a.assessment_id=".$id;
			$compdef=UlsMenu::callpdorow($query);
		}
		return $compdef;
	}
}