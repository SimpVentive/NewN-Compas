<?php

/**
 * UlsAssessmentFeedEmployees
 * 
 * This class has been auto-generated by the Doctrine ORM Framework
 * 
 * @package    ##PACKAGE##
 * @subpackage ##SUBPACKAGE##
 * @author     ##NAME## <##EMAIL##>
 * @version    SVN: $Id: Builder.php 6820 2009-11-30 17:27:49Z jwage $
 */
class UlsAssessmentFeedEmployees extends BaseUlsAssessmentFeedEmployees
{
	function preInsert($event){
		$CI =& get_instance();
		$CI->load->library('session');
        $this->created_by=$CI->session->userdata('username');
        $this->created_date=date('Y-m-d');
        $this->modified_by=$CI->session->userdata('username');        
        $this->modified_date=date('Y-m-d');
        $this->last_modified_id=$CI->session->userdata('user_id');
        $this->parent_org_id=$CI->session->userdata('parent_org_id');
    }
	
    function preUpdate($event){
		$CI =& get_instance();
		$CI->load->library('session');
        $this->modified_date=date('Y-m-d');
        $this->modified_by=$CI->session->userdata('username');
        $this->last_modified_id=$CI->session->userdata('user_id');
    }
	
	static function getfeed_assessment($id,$pos_id,$emp_id){
		$CI =& get_instance();
		$CI->load->library('session');
		$compdef=array();
		if(!empty($id) && is_numeric($id)){
			$query="select a.* from uls_assessment_feed_employees a
			where 1 and a.parent_org_id=".$CI->session->userdata('parent_org_id')." and a.assessment_id=".$id."  and a.position_id=".$pos_id." and a.employee_id=".$emp_id;
			$compdef=UlsMenu::callpdorow($query);
		}
		return $compdef;
	}
	
	static function get_feed_empothers($emp_id){
		$CI =& get_instance();
		$CI->load->library('session');
		$compdef=array();
		if(!empty($emp_id)){
			$query="select a.*,b.`assessment_name`,b.`start_date`,b.`end_date`,c.`ass_start_date`,c.`ass_end_date` from uls_assessment_feed_employees a
			left join(SELECT `assessment_id`,`assessment_name`,`start_date`,`end_date` FROM `uls_assessment_definition`) b on b.assessment_id=a.assessment_id
			left join(SELECT `position_id`,`assessment_id`,`assessment_type`,`ass_start_date`,`ass_end_date` FROM `uls_assessment_test` where assessment_type='FEEDBACK') c on a.assessment_id=c.assessment_id and a.position_id=c.position_id
			where 1 and a.parent_org_id=".$CI->session->userdata('parent_org_id')." and a.employee_id!=".$emp_id;
			$compdef=UlsMenu::callpdo($query);
		}
		return $compdef;
	}
	
	static function fetch_group($id){
		$CI =& get_instance();
		$CI->load->library('session');
		$compdef=array();
		if(!empty($id) && is_numeric($id)){
			$query="select a.* from uls_assessment_feed_employees a
			where 1 and a.parent_org_id=".$CI->session->userdata('parent_org_id')." and a.group_id=".$id;
			$compdef=UlsMenu::callpdorow($query);
		}
		return $compdef;
	}
	
	static function getfeed_assessment_details($id,$pos_id){
		$CI =& get_instance();
		$CI->load->library('session');
		$compdef=array();
		if(!empty($id) && is_numeric($id)){
			$query="select a.*,e.`employee_number`,e.`full_name` from uls_assessment_feed_employees a
			left join(SELECT `employee_id`,`employee_number`,`full_name` FROM `employee_data`) e on e.employee_id=a.employee_id
			where 1 and a.parent_org_id=".$CI->session->userdata('parent_org_id')." and a.assessment_id=".$id."  and a.position_id=".$pos_id;
			$compdef=UlsMenu::callpdo($query);
		}
		return $compdef;
	}
	
	static function get_feed_assessment_details($id){
		$CI =& get_instance();
		$CI->load->library('session');
		$compdef=array();
		if(!empty($id) && is_numeric($id)){
			$query="select a.*,e.`employee_number`,e.`full_name` from uls_assessment_feed_employees a
			left join(SELECT `employee_id`,`employee_number`,`full_name` FROM `employee_data`) e on e.employee_id=a.employee_id
			where 1 and a.parent_org_id=".$CI->session->userdata('parent_org_id')." and a.assessment_id=".$id;
			$compdef=UlsMenu::callpdo($query);
		}
		return $compdef;
	}
	
	static function get_feed_assessment_details_assess_emp($id,$emp_id){
		$CI =& get_instance();
		$CI->load->library('session');
		$compdef=array();
		if(!empty($id) && is_numeric($id)){
			$query="select a.*,e.`employee_number`,e.`full_name` from uls_assessment_feed_employees a
			left join(SELECT `employee_id`,`employee_number`,`full_name` FROM `employee_data`) e on e.employee_id=a.employee_id
			where 1 and a.parent_org_id=".$CI->session->userdata('parent_org_id')." and a.assessment_id=".$id." and a.employee_id=".$emp_id;
			$compdef=UlsMenu::callpdorow($query);
		}
		return $compdef;
	}
	
	static function get_feed_giver_assessment_details($id){
		$CI =& get_instance();
		$CI->load->library('session');
		$compdef=array();
		if(!empty($id) && is_numeric($id)){
			$query="select a.* from uls_assessment_feed_employees a
			where 1 and a.parent_org_id=".$CI->session->userdata('parent_org_id')." and a.assessment_id=".$id;
			$compdef=UlsMenu::callpdo($query);
		}
		return $compdef;
	}
	
	static function get_feed_giver_assessment_detail_emp($id,$emp_id){
		$CI =& get_instance();
		$CI->load->library('session');
		$compdef=array();
		if(!empty($id) && is_numeric($id)){
			$query="select a.* from uls_assessment_feed_employees a
			where 1 and a.parent_org_id=".$CI->session->userdata('parent_org_id')." and a.assessment_id=".$id." and a.employee_id=".$emp_id;
			$compdef=UlsMenu::callpdo($query);
		}
		return $compdef;
	}
}