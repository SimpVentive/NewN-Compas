<?php

/**
 * UlsAssessmentReportBytypeInterview
 * 
 * This class has been auto-generated by the Doctrine ORM Framework
 * 
 * @package    ##PACKAGE##
 * @subpackage ##SUBPACKAGE##
 * @author     ##NAME## <##EMAIL##>
 * @version    SVN: $Id: Builder.php 6820 2009-11-30 17:27:49Z jwage $
 */
class UlsAssessmentReportBytypeInterview extends BaseUlsAssessmentReportBytypeInterview
{
	function preInsert($event){
		 $CI =& get_instance();
		$CI->load->library('session');
        $this->created_by=$CI->session->userdata('username');
        $this->created_date=date('Y-m-d');
        $this->modified_by=$CI->session->userdata('username');        
        $this->modified_date=date('Y-m-d');
        $this->last_modified_by=$CI->session->userdata('user_id');
        $this->parent_org_id=$CI->session->userdata('parent_org_id');
    }
	
    function preUpdate($event){
		$CI =& get_instance();
		$CI->load->library('session');
        $this->modified_date=date('Y-m-d');
        $this->modified_by=$CI->session->userdata('username');
        $this->last_modified_by=$CI->session->userdata('user_id');
    }


	static function get_interview_adminassessment_com_insert($id,$pos_id,$emp_id,$assessor_id,$comp_id,$comp_int_qid,$report_id){
		$CI =& get_instance();
		$CI->load->library('session');
		$compdef=array();
		if(!empty($id) && is_numeric($id)){
			$query="select a.* from uls_assessment_report_bytype_interview a
			where 1 and a.parent_org_id=".$CI->session->userdata('parent_org_id')." and a.assessment_id=$id and a.position_id=$pos_id and a.employee_id=$emp_id and a.competency_id=$comp_id and a.assessor_id=".$assessor_id." and a.report_id=".$report_id." and a.comp_int_qid=".$comp_int_qid."";
			$compdef=UlsMenu::callpdorow($query);
		}
		return $compdef;
	}
	
	static function get_interview_question_countdetails($report_id,$assessment_id,$position_id,$employee_id,$assessor_id,$competency_id,$type_n,$int_id){
		$CI =& get_instance();
		$CI->load->library('session');
		$compdef=array();
		if(!empty($report_id) && is_numeric($report_id)){
			$query="select count(*) as total_count from uls_assessment_report_bytype_interview a
			where 1 and  a.parent_org_id=".$CI->session->userdata('parent_org_id')." and  a.report_id=".$report_id." and a.assessment_id=".$assessment_id." and a.position_id=".$position_id." and a.employee_id=".$employee_id." and a.assessor_id=".$assessor_id." and a.competency_id=".$competency_id." and a.comp_int_id=".$int_id." and a.comp_qtype='".$type_n."'";
			$compdef=UlsMenu::callpdorow($query);
		}
		return $compdef;
	}
	
	static function get_interview_check_condition($id,$pos_id,$emp_id,$assessor_id,$comp_id,$scale_id,$report_id,$type){
		$CI =& get_instance();
		$CI->load->library('session');
		$compdef=array();
		if(!empty($id) && is_numeric($id)){
			$query="select a.* from uls_assessment_report_bytype_interview a
			where 1 and a.parent_org_id=".$CI->session->userdata('parent_org_id')." and a.assessment_id=$id and a.position_id=$pos_id and a.employee_id=$emp_id and a.competency_id=$comp_id and a.assessor_id=".$assessor_id." and a.report_id=".$report_id." and a.comp_qtype='".$type."'  and a.require_scale_id=".$scale_id."";
			$compdef=UlsMenu::callpdorow($query);
		}
		return $compdef;
	}
}